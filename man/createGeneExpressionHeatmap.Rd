% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/heatmapPlot.R
\name{createGeneExpressionHeatmap}
\alias{createGeneExpressionHeatmap}
\title{Create Interactive Gene Expression Heatmap}
\usage{
createGeneExpressionHeatmap(
  df,
  samples_groups,
  heatmap_heights,
  id_or_sym = "Gene symbol",
  heatmap_colors = "App colors",
  group_colors = "inferno",
  dendrogram_type = "Samples and genes"
)
}
\arguments{
\item{df}{A data frame formatted by \code{\link{formatForHeatmap}()}.}

\item{samples_groups}{A data frame mapping sample names to group labels for column annotations.}

\item{heatmap_heights}{A list of height and domain settings for the heatmap and its components, created by \code{\link{heatmapHeights}()}.}

\item{id_or_sym}{Character. The gene identifier (EntrezID, Ensembl ID, or even the "long" gene name) or the gene symbol to use as row names in the heatmap. Options are "Ensembl ID", "Entrez ID", "Gene symbol", and "Gene name". Defaults to "Gene symbol".}

\item{heatmap_colors}{Character. The color palette to use for the heatmap tiles. Defaults to "App colors".}

\item{group_colors}{Character. The color palette to use for group labels. Defaults to "inferno".}

\item{dendrogram_type}{Character. Dendrogram display option: "Samples", "Genes", "Samples and genes", or "None". Defaults to "Samples and genes".}
}
\value{
The interactive heatmap as a \code{plotly} object.
}
\description{
Generates an interactive gene expression heatmap using \code{\link[heatmaply]{heatmaply}()}, with custom color palettes, group annotations, dendrogram options, and detailed tooltips for each cell. Supports dynamic row/column clustering, custom group colors, and advanced layout adjustments for publication-quality visualization.
}
