% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/volcanoPlot.R
\name{createVolcanoPlot}
\alias{createVolcanoPlot}
\title{Create a Volcano Plot}
\usage{
createVolcanoPlot(
  df,
  p_threshold = "0.05",
  l2fc_threshold = 1,
  selected_genes = c(),
  selected_contrast = "all",
  standalone = FALSE,
  color_up = get_theme_colors(color = "pink"),
  color_down = get_theme_colors(color = "blue"),
  highlight_top = TRUE,
  dot_size = 1.5
)
}
\arguments{
\item{df}{A data frame containing gene expression results, including columns for gene symbols, log2 fold change, adjusted p-values, and contrast labels.}

\item{p_threshold}{Numeric or character. The adjusted p-value threshold for significance.}

\item{l2fc_threshold}{Numeric. The log2 fold-change threshold for significance.}

\item{selected_genes}{Character vector. Gene symbols to highlight across all contrasts similarly.}

\item{selected_contrast}{Character vector. Contrast names to display in the plot facets.}

\item{standalone}{Logical. If \code{TRUE}, the PCA plot is generated as a standalone plot, which is not interactive. If \code{FALSE} (required inside DExploreR), the plot is interactive via \code{plotly}. Defaults to \code{FALSE}.}

\item{color_up}{Set the color of the upregulated genes. Defaults to the app's pink color.}

\item{color_down}{Set the color of the downregulated genes. Defaults to the app's blue color.}

\item{highlight_top}{Logical. If \code{TRUE}, highlights the top differentially expressed genes per contrast. Defaults to \code{TRUE}.}

\item{dot_size}{Numeric. Size of the points in the plot. Defaults to \code{1.5}.}
}
\value{
The interactive volcano plot as a \code{plotly} object.
}
\description{
Generates a volcano plot for differential gene expression analysis using \code{ggplot2} and/or \code{plotly}. Highlights significant genes based on user-defined thresholds, annotates user-selected genes, and provides detailed tooltips with gene information. Supports multiple contrasts, custom color palettes, and optimized rendering for large datasets.
}
