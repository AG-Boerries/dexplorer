function colorsWithIconChoice(data) {
      return window.colorPaletteChoicesIconsChoice[data.value] || data.label;
    }
    
function colorsWithIconSelected(data) {
      return window.colorPaletteChoicesIconsSelected[data.value] || data.label;
    }